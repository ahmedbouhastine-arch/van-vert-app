
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Van-Vert application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "User's role (e.g., user, reviewer, admin, head-admin)."
        },
        "displayName": {
          "type": "string",
          "description": "User's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "URL for user's profile photo.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the user was created."
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "createdAt"
      ]
    },
    "Application": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Application",
      "type": "object",
      "description": "Represents a pilot license application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the application entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Application)"
        },
        "licenseType": {
          "type": "string",
          "description": "The type of license being applied for."
        },
        "status": {
          "type": "string",
          "description": "Application status (e.g., draft, submitted, ready)."
        },
        "submittedAt": {
          "type": "string",
          "description": "Timestamp when the application was submitted.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the application was last updated.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the application was created.",
          "format": "date-time"
        },
        "feedback": {
          "type": "string",
          "description": "Internal notes from admins, visible to the user."
        },
        "flightLogs": {
          "type": "array",
          "description": "A list of flight logs submitted by the applicant.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "date": { "type": "string", "format": "date" },
              "duration": { "type": "number" },
              "aircraft": { "type": "string" },
              "remarks": { "type": "string" }
            },
            "required": ["id", "date", "duration", "aircraft"]
          }
        }
      },
      "required": [
        "id",
        "userId",
        "licenseType",
        "status"
      ]
    },
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents a document uploaded for an application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the document entity."
        },
        "docRequirementId": {
          "type": "string",
          "description": "Reference to the requirement definition."
        },
        "name": {
          "type": "string",
          "description": "Name of the document requirement."
        },
        "description": {
          "type": "string",
          "description": "Description of the document requirement."
        },
        "fileName": {
          "type": "string",
          "description": "Name of the uploaded file."
        },
        "fileType": {
          "type": "string",
          "description": "Type of the uploaded file (e.g., PDF, JPG, PNG)."
        },
        "fileSize": {
          "type": "number",
          "description": "Size of the uploaded file in bytes."
        },
        "storagePath": {
          "type": "string",
          "description": "Path to the file in Firebase Storage."
        },
        "expiryDate": {
          "type": "string",
          "description": "Expiry date of the document, if applicable.",
          "format": "date"
        },
        "status": {
          "type": "string",
          "description": "Document status (e.g., missing, uploaded, needs attention)."
        },
        "uploadedAt": {
          "type": "string",
          "description": "Timestamp when the document was uploaded.",
          "format": "date-time"
        },
        "isExpiringSoon": {
          "type": "boolean",
          "description": "Flag indicating if the document is expiring soon."
        },
        "requiresExpiry": {
            "type": "boolean",
            "description": "Whether this document requires an expiry date."
        }
      },
      "required": [
        "id",
        "docRequirementId",
        "name",
        "description",
        "status",
        "requiresExpiry"
      ]
    },
    "Notification": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Notification",
      "type": "object",
      "description": "Represents a notification for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique ID for the notification."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who this notification is for."
        },
        "title": {
          "type": "string",
          "description": "The title of the notification."
        },
        "body": {
          "type": "string",
          "description": "The main content of the notification."
        },
        "href": {
          "type": "string",
          "description": "A URL to navigate to when the notification is clicked."
        },
        "isRead": {
          "type": "boolean",
          "description": "Whether the user has read the notification."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the notification was created."
        }
      },
      "required": ["id", "userId", "title", "body", "isRead", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles and roles. This data is critical for role-based access control.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/applications/{applicationId}",
        "definition": {
          "entityName": "Application",
          "schema": {
            "$ref": "#/backend/entities/Application"
          },
          "description": "Top-level collection for all applications. User-specific access is controlled by security rules based on the 'userId' field in the document. Admins have broader access.",
          "params": [
            {
              "name": "applicationId",
              "description": "The unique identifier for the application."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/notifications/{notificationId}",
        "definition": {
          "entityName": "Notification",
          "schema": { "$ref": "#/backend/entities/Notification" },
          "description": "Subcollection storing notifications for a specific user. This ensures notifications are private and queries are efficient.",
          "params": [
             {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "notificationId",
              "description": "The unique identifier for the notification."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure has been refactored to support administrative roles efficiently. Applications are now in a top-level collection to allow admins to query across all submissions, which is not feasible with a nested structure. Security is maintained through Firestore rules that check user roles and document ownership.\n\n*   **Users (Collection: `/users/{userId}`):** Stores user profiles and roles. This collection is the source of truth for role-based access control (RBAC).\n\n*   **Applications (Collection: `/applications/{applicationId}`):** A flat collection for all applications. A user can only access their own applications (verified via the `userId` field in the document). Admins can list and view all applications. Documents are stored as an array within each application document, simplifying data retrieval.\n\n* **Notifications (Subcollection: `/users/{userId}/notifications/{notificationId}`):** A subcollection under each user to store their personal notifications. This is a secure and scalable pattern for user-specific data."
  }
}
